$kind: http-request
url: "{{baseUrl}}/health"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Agent is healthy', () => {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4000
